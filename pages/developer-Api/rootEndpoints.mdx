---
title: Root Endpoints
---
import ApiSection from '../../src/app/_components/developerApiContent/apiTester'

# Root Endpoint

Here’s a quick rundown of the core API categories we offer. Each section links to a dedicated page with in-depth details on endpoints, request/response formats, and usage examples.

## User Data: Retrieval & Insertion

### Cards & Consents

- **List Installed Cards**
  Retrieve any brand or loyalty “cards” a user has installed.  
  <ApiSection 
  endpoint={{
    method: "POST",
    path: "/api/v1/list-installed-cards",
    description: "Retrieve any brand or loyalty 'cards' a user has installed.",
    queryParams: [
      { name: "phone_number", required: true, placeholder: "Enter phone number" }
    ],
    headers: [
      { name: "token", required: true, placeholder: "Enter token" }
    ]
  }}
  />


- **Request Consent**
  Send a request prompting the user to grant or reject data access.  
  <ApiSection 
  endpoint={{
    method: "POST",
    path: "/api/v1/request-consent",
    description: "Send a request prompting the user to grant or reject data access.",
    requestBody: {
      phone_number: { type: "string", placeholder: "Enter phone number" },
      access_token: { type: "string", placeholder: "Enter access token" },
      card_name: { type: "string", placeholder: "Enter card name" },
      expiry: { type: "string", placeholder: "Enter expiry date" },
      bid_value: { type: "number", placeholder: "Enter bid value" }
    }
  }}
  />

- **Get Consent**
  Retrieves a list of cards for which the user has already granted consent.  
  <ApiSection 
  endpoint={{
    method: "POST",
    path: "/api/v1/list-consented-cards",
    description: "Get a list of consented cards",
    queryParams: [
      { name: "phone_number", required: true, placeholder: "Enter phone number" }
    ],
    headers: [
      { name: "token", required: true, placeholder: "Enter token" }
    ],
    
  }}
  />

### Receipts

- **Insert Receipt Data**
  Add new purchase details (brand, location, cost) to the user’s record.  
  <ApiSection 
  endpoint={{
    method: "POST",
    path: "/api/v1/list-consented-cards",
    description: "Insert your receipt data",
    queryParams: [
      { name: "phone_number", required: true, placeholder: "Enter phone number" }
    ],
    headers: [
      { name: "token", required: true, placeholder: "Enter token" }
    ],
    requestBody: {
      brand: { type: "string", placeholder: "Enter website url" },
      location: { type: "string", placeholder: "Enter the last visited time" },
      purchase_category: { type: "string", placeholder: "Enter the last visited time" },
      brand_category: { type: "string", placeholder: "Enter your interest keywords" },
      Date: { type: "string", placeholder: "Enter brand name" },
      currency: { type: "string", placeholder: "Enter the name of your source" },
      payment_method: { type: "string", placeholder: "Enter the name of your source" },
      total_cost: { type: "number", placeholder: "Enter number of product clicks" },
      inr_total_cost: { type: "number", placeholder: "Enter number of product clicks" },
      usd_total_cost: { type: "number", placeholder: "Enter number of product clicks" },
    }
  }}
  />

- **Get Receipt Data**
  Fetch all receipt entries a user has shared for analysis or personalization.  
  <ApiSection 
  endpoint={{
    method: "POST",
    path: "/api/v1/get-receipt-data",
    description: "Get user's receipt data",
    queryParams: [
      { name: "phone_number", required: true, placeholder: "Enter phone number" }
    ],
    headers: [
      { name: "token", required: true, placeholder: "Enter token" }
    ]
  }}
  />


### Health

- **Insert Health Data**
  Store user-submitted health metrics (steps, surveys, etc.).  
  <ApiSection 
    endpoint={{
    method: "POST",
    path: "/api/v1/insert-health-data",
    description: "Insert and submit health data endpoints",
    queryParams: [
      { name: "phone_number", required: true, placeholder: "Enter phone number" }
    ],
    headers: [
      { name: "token", required: true, placeholder: "Enter token" }
    ],
    requestBody: {
      answers: {
        type: "array",
        items: {
          type: "object",
          additionalProperties: { type: "string" }
        }
      }
    }
    }}
  />

- **Get Health Data**
  Retrieve health-related information for insights or wellness applications.  
  <ApiSection 
  endpoint={{
    method: "POST",
    path: "/api/v1/health-data",
    description: "Retrieve health data information for insights",
    queryParams: [
      { name: "phone_number", required: true, placeholder: "Enter phone number" }
    ],
    headers: [
      { name: "token", required: true, placeholder: "Enter token" }
    ]
  }}
  />

### Browsing

- **Insert Browsing Data**
  Record user’s browsing activity (URLs visited, clicks, interest keywords).  
  <ApiSection 
  endpoint={{
    method: "POST",
    path: "/api/v1/insert-browsing-data",
    description: "Insert your browsing data endpoints",
    queryParams: [
      { name: "phone_number", required: true, placeholder: "Enter phone number" }
    ],
    headers: [
      { name: "token", required: true, placeholder: "Enter token" }
    ],
    requestBody: {
      website_url: { type: "string", placeholder: "Enter website url" },
      visit_time: { type: "string", placeholder: "Enter the last visited time" },
      product_clicks: { type: "number", placeholder: "Enter number of product clicks" },
      interest_keywords: { type: "string", placeholder: "Enter your interest keywords" },
      brand: { type: "string", placeholder: "Enter brand name" },
      source: { type: "string", placeholder: "Enter the name of your source" },
      duration: { type: "number", placeholder: "Enter total duration" },
    }
  }}
  />

- **Get Browsing Data**
  Access previously saved browsing records for content personalization.  
  <ApiSection 
  endpoint={{
    method: "POST",
    path: "/api/v1/browsing-data",
    description: "Get Browsing Data Endpoints",
    queryParams: [
      { name: "phone_number", required: true, placeholder: "Enter phone number" }
    ],
    headers: [
      { name: "token", required: true, placeholder: "Enter token" }
    ]
  }}
  />

### Brand Preferences

- **Insert Brand Preference Data**
  Capture user preferences for specific brands.  
  <ApiSection 
    endpoint={{
    method: "POST",
    path: "/api/v1/insert-brand-preference-data",
    description: "Insert Brand Preference Data Endpoints",
    queryParams: [
      { name: "phone_number", required: true, placeholder: "Enter phone number" },
      { name: "brand_name", required: true, placeholder: "Enter your brand name" },
    ],
    headers: [
      { name: "token", required: true, placeholder: "Enter token" }
    ],
    requestBody: {
      answers: {
        type: "array",
        items: {
          type: "object",
          additionalProperties: { type: "string" }
        }
      }
    }
  }}
  />

- **Get Brand Preferences**
  Retrieve stored brand preferences (favorite colors, styles, features).  
  <ApiSection 
    endpoint={{
    method: "POST",
    path: "/api/v1/brand-preferences",
    description: "Get Brand Preference Data Endpoints",
    queryParams: [
      { name: "phone_number", required: true, placeholder: "Enter phone number" },
      { name: "brand_name", required: true, placeholder: "Enter your brand name" },
    ],
    headers: [
      { name: "token", required: true, placeholder: "Enter token" }
    ],
  }}
  />

### Fashion

- **Insert Fashion Data**
  Submit user fashion-related insights or survey answers.  
  <ApiSection 
    endpoint={{
    method: "POST",
    path: "/api/v1/insert-fashion-data",
    description: "Insert fashion data endpoints",
    queryParams: [
      { name: "phone_number", required: true, placeholder: "Enter phone number" },
    ],
    headers: [
      { name: "token", required: true, placeholder: "Enter token" }
    ],
    requestBody: {
      answers: {
        type: "array",
        items: {
          type: "object",
          additionalProperties: { type: "string" }
        }
      }
    }
  }}
  />

- **Get Fashion Data**
  Access stored style preferences, purchase patterns, or outfit logs.  
  <ApiSection 
    endpoint={{
    method: "POST",
    path: "/api/v1/fashion-data",
    description: "Get Fashion Data Endpoints",
    queryParams: [
      { name: "phone_number", required: true, placeholder: "Enter phone number" },
    ],
    headers: [
      { name: "token", required: true, placeholder: "Enter token" }
    ],
  }}
  />

### Food & Insurance

- **Get Food Data**
  Retrieve user dietary or culinary preferences.  
  <ApiSection 
    endpoint={{
    method: "POST",
    path: "/api/v1/food-data",
    description: "Get Food Data Endpoints",
    queryParams: [
      { name: "phone_number", required: true, placeholder: "Enter phone number" },
    ],
    headers: [
      { name: "token", required: true, placeholder: "Enter token" }
    ],
  }}
  />

- **Get Insurance Data**
  Access insurance-related information (policies, coverage data).  
  <ApiSection 
    endpoint={{
    method: "GET",
    path: "/api/v1/insurance-data",
    description: "Get Root Endpoints",
    queryParams: [
      { name: "phone_number", required: true, placeholder: "Enter phone number" },
    ],
    headers: [
      { name: "token", required: true, placeholder: "Enter token" }
    ],
  }}
  />

## Root

- **Root Endpoint**
  A simple check to confirm the API is online and receiving requests.  
   <ApiSection 
    endpoint={{
    method: "GET",
    path: "/",
    description: "Get Root Endpoints",
  }}
  />

---

### Ready to dive deeper?

- Visit the **Setup & Authentication** page to learn how to create your profile, generate an API key, and authenticate.
- Explore the **Data Insertion & Retrieval** pages for detailed endpoint documentation, example requests, and response schemas.
- Check out the **Consent Flow** guidelines to understand how to handle user permissions when accessing sensitive data.
- If you have any questions, contact us at [info@hush1one.com](mailto:info@hush1one.com).


### Description:
A simple check to ensure the API is up and running. It often returns a basic message or status response indicating that the Hushh Developer API is online.

### Request:
```http
No parameters required.
```



### Success Response (200 OK):
```json
"Welcome to Hushh Developer API."
```

> *(Exact response may vary.)*

### Use Cases:

- **Health Check**: You can ping this endpoint to confirm connectivity and service availability.
- **Debugging**: Quick check if you’re receiving responses from the correct environment (test vs. live).

---

> For more details on setting up and using the API, check out the [Onboarding Guide](/onboarding) or dive into [API References](/api-reference).
